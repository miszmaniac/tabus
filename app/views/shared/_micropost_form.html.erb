<%= form_for(@micropost) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
      <%= f.text_area :content, placeholder: "Utwórz nowy wpis..." %>
    </div>
    <%= f.submit "Zapisz", class: "btn btn-large btn-primary" %>
<% end %>
